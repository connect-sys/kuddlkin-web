# Goes in the http{} context (drop into /etc/nginx/conf.d/).
# Defines $connection_upgrade used by the proxy blocks for WebSocket upgrades.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
